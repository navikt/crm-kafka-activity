apiVersion: kafka.nais.io/v1
kind: Topic
metadata:
  name: crm-kafka-activity-CHANGE-ME # ! Kafka Topic name used in other apps
  namespace: team-dialog
  labels:
    team: team-dialog
spec:
  pool: {{pool}}
  config:
  {{#each config as |value key|}}
     {{key}}: {{value}}
  {{/each}}
  acl:
    - team: team-dialog
      application: crm-kafka-activity-CHANGE-ME # ! Same as above
      access: readwrite